const config = {};

config.seedboxFTP = {
    host: "localhost",
    user: "[Default]",
    password: "[Somepassword]",
    syncRoot: "/baseMediaFolder"
};

config.localSyncFolder = "~/somePath";

module.exports = config;